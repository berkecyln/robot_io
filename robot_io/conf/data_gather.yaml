defaults:
  - robot: panda_franky_interface_policy 
  - cams: camera_manager
  - _self_                        

hydra:
  run:
    dir: ${output_dir}/logs/${now:%Y-%m-%d}/${now:%H-%M-%S}

output_dir: /home/ceylanb/DreMa/drema_project/input

movement:
  move_distance: 0.05
  num_waypoints: 150
  flip_orientation: false
  arch:
    height: 0.3
    width: 0.4